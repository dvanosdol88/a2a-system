# A2A System Dependencies
# Agent-to-Agent Communication System (Claude + Jules)
# Created: 2025-07-04

# Core API Framework
Flask==3.1.1
Werkzeug==3.1.3

# HTTP Client for API Communication
requests==2.32.4

# Testing Framework
pytest==8.4.1

# Flask Dependencies (auto-installed but listed for clarity)
blinker>=1.9.0
click>=8.1.3
itsdangerous>=2.2.0
Jinja2>=3.1.2
MarkupSafe>=2.1.1

# HTTP Client Dependencies
urllib3>=2.5.0
certifi>=2025.6.15
charset-normalizer>=3.4.2
idna>=3.10

# Testing Dependencies
iniconfig>=2.1.0
packaging>=25.0
pluggy>=1.6.0
pygments>=2.19.2

# Enhanced Testing Framework Dependencies
asyncio  # For async testing capabilities (built-in Python 3.4+)
threading  # For concurrent testing (built-in)
dataclasses  # For test data structures (built-in Python 3.7+)
enum  # For test enums (built-in)
uuid  # For message IDs (built-in)
datetime  # For timestamps (built-in)
pathlib  # For file handling (built-in Python 3.4+)
json  # For data serialization (built-in)
subprocess  # For test orchestration (built-in)

# Note: Most enhanced testing dependencies are built-in Python modules
# The enhanced testing framework uses only standard library modules beyond the core dependencies

# Monitoring Dashboard Dependencies
psutil>=5.9.0  # System resource monitoring
# Flask already included above for monitoring dashboard server

# Additional monitoring capabilities (all built-in):
# time, threading, statistics, pathlib, json, subprocess